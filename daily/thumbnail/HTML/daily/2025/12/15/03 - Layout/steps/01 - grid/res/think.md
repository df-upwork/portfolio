https://gemini.google.com/share/ce1db97436b2

# 1

Замени CSS-правило для `body` на следующее:

```css
body {
	background-color: #ffffcc;
	display: grid;
	font-family: 'Carlito';
	grid-template-columns: 38.28px 1fr;
	height: 800px;
	margin: 0;
	overflow: hidden;
	padding: 0;
	width: 1000px;
}
```

# 2

Замени CSS-правило для `#sidebar` на следующее:

```css
#sidebar {
	background-color: #ff6133;
}
```

# 3

Замени CSS-правило для `#content` на следующее:

```css
#content {
	align-self: center;
	line-height: 1.1;
	text-align: center;
	white-space: nowrap;
}
```